# Nola Tech SaaS — Static Site

This repository contains a minimal static landing page for **Nola Tech SaaS** (HTML/CSS/JS), focused on custom SaaS products for small business owners.

Files:
- [index.html](index.html) — main landing page
- [styles.css](styles.css) — redesigned styles
- [script.js](script.js) — small form behavior

Preview locally:

Using Python 3 (recommended):

```powershell
python -m http.server 8000
```

Then open http://localhost:8000 in your browser.

Or open `index.html` directly in your browser. For live reload use the VS Code Live Server extension.

Next steps you might want:
- Replace placeholder testimonials with real customer quotes.
- Add a logo image at `assets/logo.png` and update the `.logo` block in `styles.css`.
- Connect the contact form to a serverless function or email gateway.